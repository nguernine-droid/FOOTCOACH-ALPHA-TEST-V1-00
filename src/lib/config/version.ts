/**
 * Source unique de vérité pour la version applicative.
 * Comparée à app_config.min_version côté serveur par VersionGuard (blocage)
 * et TopBar (indicateur d'état).
 */
export const CURRENT_APP_VERSION = '1.0.341';
